# @chemistry/sync-orchestrator

Orchestrates the sync process.
    - Send events to corresponding queues
    - Add logs to corresponding tables

## Commands

Build Container

```bash
docker build .
```

## Local Commands

* Build project: `npm run build`
